FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev

COPY . .

EXPOSE 4000

# Před spuštěním serveru se vždy provede (idempotentní) migrace schématu.
CMD ["sh", "-c", "node src/migrate.js && node src/server.js"]
