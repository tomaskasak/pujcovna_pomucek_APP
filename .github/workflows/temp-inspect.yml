name: TEMP inspect pricelist page

on:
  workflow_dispatch: {}

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch inspect-pricelist
        run: |
          curl -sS "https://pujcovna-backend.onrender.com/api/cron/inspect-pricelist?secret=${{ secrets.CRON_SECRET }}" | python3 -m json.tool
