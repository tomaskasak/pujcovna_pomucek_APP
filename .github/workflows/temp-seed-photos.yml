name: TEMP seed item photos from website

on:
  workflow_dispatch: {}

jobs:
  seed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stáhnout a poslat fotky appce
        env:
          APP_URL: https://pujcovna-backend.onrender.com
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: python3 scripts/seed-photos.py
